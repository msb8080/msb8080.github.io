{
  "name": "msb8080-github-io",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "check": "node --check js/homepage-v2.js && node --check js/homepage-actions.js && node scripts/verify-homepage.mjs",
    "build:blog": "npm --prefix blog-src run build && rsync -a --delete --exclude='.DS_Store' blog-src/dist/ blog/",
    "astro:check": "astro check",
    "build:homepage": "astro build --outDir .astro-dist && cp .astro-dist/index.html index.html && rsync -a .astro-dist/_astro/ _astro/",
    "build:site": "npm run build:blog && npm run build:homepage && npm run check"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^5.18.2",
    "typescript": "^5.9.3"
  }
}
